# Required for persistent storage / ingestion
DATABASE_URL=postgresql://user:password@host:5432/enclave

# Set to "disable" only for a trusted local development database.
DATABASE_SSL=require

# Optional deployment URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
